import { navigationSlice } from '../navigation/navigation.slice'
import { settingsSlice } from '../settings/settings.slice'

export const { navigationSetHamburger } = navigationSlice.actions
export const { settingsSetTheme } = settingsSlice.actions
